package com.example.ontrack.task;

import javafx.event.ActionEvent;
import javafx.event.EventHandler;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.TextField;
import javafx.scene.layout.GridPane;

public class RepetitionRuleFormController {


    /*
    @FXML
    private void addNewIterationRow()
    {
        Label roundNumberLabel = new Label(String.valueOf(rowCount+1));
        TextField intervalTextField = new TextField("Test2");
        Button removeIterationButton = new Button("Remove");
        EventHandler<ActionEvent> removeRow = new EventHandler<ActionEvent>() {
            @Override
            public void handle(ActionEvent actionEvent) {
                iterationGridPane.getChildren().removeAll(roundNumberLabel,intervalTextField,removeIterationButton);
                rowCount--;
            }
        };
        removeIterationButton.setOnAction(removeRow);
        iterationGridPane.addRow(rowCount+1,roundNumberLabel,intervalTextField,removeIterationButton);
        rowCount++;
    }*/
}
